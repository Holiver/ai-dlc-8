server:
  port: "8080"
  mode: "debug"

database:
  host: "localhost"
  port: "3306"
  user: "root"
  password: "password"
  dbname: "awsome_shop"
  charset: "utf8mb4"

jwt:
  secret: "your-secret-key-change-in-production"
  expirationhours: 24
